package org.opentripplanner.middleware.models;

/**
 * Describes the means to notify a user:
 * none (no notification), via email, via sms).
 */
public enum UserNotificationChannel {
    NONE, EMAIL, SMS
}
